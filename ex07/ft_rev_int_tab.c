#include <unistd.h>
#include <stdio.h>

void    ft_rev_int_tab(int *tab, int size)
{   
    int temp;
    int counter;

          
}

int main()
{   
    int tab[size = 10] = {1, 2, 3, 4, 5, 6, 7, 8, 9}; 
    
    size - 1 = temp;
    // take 1 put in temp
    // take 9 put in 1
    temp put in 9
    // take 2 put in temp
    // take 8 put in 2
    temp put in 8
    // take 3 put in temp
    // take 7 put in 3
    temp put in 7
    // take 4 put in temp
    // take 6 put in 4
    temp put in 6
    
    // outcome {9 8 7 6 5 4 3 2 1} 
    
    ft_rev_int_tab(&tab, 9);    

    return 0;
}